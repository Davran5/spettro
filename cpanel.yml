---
deployment:
  tasks:
    - export DEPLOYPATH=/home/YOUR_CPANEL_USERNAME/public_html/
    - cd $REPOPATH
    - /usr/local/bin/npm install
    - /usr/local/bin/npm run build
    - /bin/rsync -aP --delete $REPOPATH/dist/ $DEPLOYPATH
